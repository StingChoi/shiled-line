# 측정 설치 가이드 (GA4 + 네이버 애널리틱스)

작성 2026-08-20 · 대상 `www.shiled-line.com`

코드는 **이미 다 심어놨습니다.** 홈페이지 286개 페이지(홈·FAQ·코팅원리·블로그 282편) 전부에
`/assets/js/analytics.js` 가 들어가 있습니다.

**ID 2개도 입력 완료(2026-08-20)** 되어 측정이 켜져 있습니다.
이제 남은 건 **배포**, 그리고 배포 후 GA4에서 전환 등록·서치콘솔 연결(STEP 4~5)뿐입니다.

---

## STEP 1~3 · ID 발급 및 입력 — ✅ 완료 (2026-08-20)

두 개 모두 발급받아 `assets/js/analytics.js` 에 입력을 마쳤습니다. **더 하실 일 없습니다.**

| 항목 | 값 | 확인 위치 |
|---|---|---|
| **GA4 측정 ID** | `G-CQY0GHZH3G` | analytics.google.com → 관리 → 데이터 스트림 |
| **네이버 발급ID** | `1bd0c6016501f40` | analytics.naver.com → 톱니바퀴 ⚙ → 사이트정보 |

**네이버 쪽 참고** — 사이트는 이미 등록되어 있었습니다(사이트명 `쉴드광택`, URL `https://www.shiled-line.com/`).
등록만 해두고 스크립트를 안 심은 상태였습니다. 그래서 그동안 데이터가 0이었습니다.

⚠️ **네이버 발급ID는 `s_` 로 시작하지 않습니다.** `1bd0c6016501f40` 같은 16진 문자열입니다.
처음에 검사식을 `s_` 로 좁게 잡아 멀쩡한 키가 거부되는 문제가 있어, 영숫자 8자 이상을 받도록 고쳤습니다.
스크립트 주소도 네이버가 발급한 스니펫과 동일하게 `wcs.pstatic.net` 으로 맞췄습니다.

**로컬 검증 결과 (2026-08-20)**

```
GA4 스크립트        OK   G-CQY0GHZH3G
page_view           OK
generate_lead       OK   {method:"tel", page_type:"home"}
네이버 wcslog.js    OK   wcs 객체 로드
wcs_add             OK   {"wa":"1bd0c6016501f40"}
네이버 전환 생성    OK   cnvType=2|cnvValue=1
```

나중에 ID를 바꿀 일이 생기면 `assets/js/analytics.js` 맨 위 CONFIG 세 줄만 고치면 됩니다.

---

## STEP 4 · 전화·카톡 클릭을 "전환"으로 등록 (GA4에서 1회)

이걸 해야 **"이번 달 문의 몇 건"** 이 숫자로 보입니다.

1. GA4 → **관리 → 이벤트**
2. 목록에 `generate_lead` 가 보일 때까지 기다립니다
   (전화나 카톡 버튼을 실제로 한 번 눌러야 생깁니다. 배포 후 직접 눌러보세요. 최대 24시간)
3. `generate_lead` 오른쪽 **"키 이벤트로 표시"** 토글을 켭니다

이제 GA4 보고서에서 이렇게 볼 수 있습니다.

| 보고서 위치 | 알 수 있는 것 |
|---|---|
| 보고서 → 참여도 → 이벤트 | 전화 클릭 / 카톡 클릭 건수 |
| 보고서 → 참여도 → 페이지 | **어떤 블로그 글이 문의를 만들었는지** |
| 보고서 → 획득 → 트래픽 획득 | 네이버·구글·인스타 중 어디서 오는지 |

`generate_lead` 이벤트에는 `method`(tel/kakao)와 `page_type`(blog_post/price/home…)이
같이 담기므로, **"블로그에서 온 문의 vs 가격페이지에서 온 문의"** 도 나눠 볼 수 있습니다.

---

## STEP 5 · 서치콘솔 / 서치어드바이저 연결

두 곳 다 **소유확인과 sitemap 제출은 이미 완료**되어 있습니다(2026-07-06).
남은 건 애널리틱스와 이어붙이는 것뿐입니다.

**구글 (GA4 ↔ 서치콘솔)**
1. GA4 → **관리 → 제품 링크 → Search Console 링크**
2. **연결** → `www.shiled-line.com` 속성 선택 → 웹 스트림 선택 → 확인
3. 연결 후 **보고서 → 라이브러리** 에서 `Search Console` 컬렉션을 **게시**
   → 그래야 왼쪽 메뉴에 검색어 보고서가 나타납니다

이걸 연결하면 **"어떤 검색어로 들어와서 → 문의까지 갔는가"** 가 한 화면에서 보입니다.
지금 리포트에서 지적한 "유막제거 키워드를 파야 한다"는 판단도, 연결 후엔 추측이 아니라
숫자로 확인하며 조정할 수 있습니다.

**네이버 (애널리틱스 ↔ 서치어드바이저)**
- 네이버는 자동 연동이라 별도 작업이 없습니다.
- <https://searchadvisor.naver.com> → 사이트 → **검색 반영 현황 / 검색 유입** 에서
  유입 검색어를 확인하면 됩니다.

---

## STEP 6 · 잘 붙었는지 확인하는 법

**즉시 확인 (5분)**
1. `analytics.js` 안 `DEBUG: false` 를 `DEBUG: true` 로 바꾸고 배포
2. 홈페이지 접속 → 키보드 `F12` → **Console** 탭
3. `[측정] 측정 스크립트 준비 완료 · GA4:ON · 네이버:ON` 이 보이면 성공
4. 전화 버튼을 눌러 `[측정] generate_lead {method: "tel" …}` 이 찍히는지 확인
5. 확인 끝나면 `DEBUG: false` 로 되돌리기

**GA4에서 확인 (실시간)**
- GA4 → **보고서 → 실시간**
- 본인 휴대폰으로 홈페이지 접속 → 30초 안에 사용자 1명이 잡히면 정상

---

## 무엇이 자동으로 잡히는가 (참고)

| 이벤트 이름 | 언제 발생 | 담기는 정보 |
|---|---|---|
| `page_view` | 모든 페이지 조회 (`#/price` 같은 화면 이동 포함) | 주소, 제목 |
| `generate_lead` | **전화번호 클릭** | `method: tel` |
| `generate_lead` | **카카오톡 클릭** | `method: kakao` |
| `outbound_click` | 네이버 플레이스/블로그/유튜브/인스타 클릭 | 목적지 |
| `price_calculate` | 가격 계산기에서 금액 확인 | 차급, 시공내용, 금액 |
| `cert_lookup` | 시공증명서 조회 | — |

모든 이벤트에 `page_type` 이 붙습니다 → `home` / `blog_post` / `price` / `faq` / `coating`

네이버 애널리틱스 쪽은 전화·카톡 클릭 시 **"신청/예약" 전환**으로 함께 보고됩니다.

---

## 주의

- `analytics.js` 의 CONFIG 세 줄 말고는 **건드리지 마세요.**
- 측정 ID를 잘못 넣으면(형식이 안 맞으면) 스크립트가 스스로 꺼집니다. 사이트가 깨지지는 않습니다.
- 개인정보를 수집하지 않습니다(전화번호·이름 등을 전송하지 않음). 클릭 여부만 셉니다.
